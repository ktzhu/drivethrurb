doctype html
html 
	head
		title DriveThru | News Feed
		script src="/javascripts/feed.js"
	body
		div class="navBar" 
			= link_to 'DriveThru', root_path, :rel => 'external'

		
		div id="storyWrapper"
				- @articles.each do |article|
					= sanitize("Did you know?\n")
					h2 = article.title
					h3 = article.abstract
					p = GuardianContent.truncated_text(GuardianContent.extract_lede( article.url ))

				div class="finish"
						a href="/" Cool, I'm done.
